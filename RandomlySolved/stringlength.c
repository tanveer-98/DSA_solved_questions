#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include<ctype.h>
int main()
{
    int len,one=0,two=0,three=0,zero=0,four=0,five=0,six=0,seven=0,eight=0,nine=0;
    char string[100];
    scanf("%s",&string);
    len=strlen(string);
    printf("%d",len);
    return 0;
    }
